#!/bin/bash

# Модуль SSH безопасности

# Конфигурация по умолчанию
readonly DEFAULT_SSH_PORT=2222
readonly SSHD_CONFIG="/etc/ssh/sshd_config"

# Смена SSH порта
change_ssh_port() {
    log_info "Функция смены SSH порта будет реализована"
}

# Отключение парольной авторизации  
disable_password_auth() {
    log_info "Функция отключения парольной авторизации будет реализована"
}

# Настройка SSH ключей
setup_ssh_keys() {
    log_info "Функция настройки SSH ключей будет реализована"
}

